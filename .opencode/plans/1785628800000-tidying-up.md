# repo-wiki 未完成项清零实现计划（T0-T5）

> 基于：未完成项深度分析报告（m00510，双并行子代理 A/B 审计）+ 删除面/接缝深度检索（m00515 两子代理）+ 网络查证（cargo-machete 依赖审计实践、spawn+try_wait+timeout 阻塞命令测试模式）。
> 基线：268 测试 17 套件全绿、clippy 0 警告。项目未上线无存量用户，可不向后兼容（用户已批准）。
> 决策拍板（2026-08-02，question 工具确认）：保留 complete_stream；删 output.format 枚举；affected_modules 接线；补真实 git e2e；删三死键+status 补健康检查；评测/测试/信号量/依赖审计全部执行。

## 目的地

把审计发现的未完成项全部清零：A 类 3 项（格式死键/status 桩/残留文件）、B 类 5 项（死代码，complete_stream 除外）、C 类 4 项（死键）、D 类 3 项（信号量/评测断言/CLI 测试）+ 2 项新发现缺陷（affected_modules 未接线、test_e2e 差分盲区），使"配置必生效、代码无死能力、增量真差分、评测可回归"成为可验证状态。

## 关键事实（已验证，非猜测）

| # | 事实 | 证据 |
|---|------|------|
| F1 | complete_stream 全链仅 llm.rs 内 9 处命中，生产零调用（用户拍板保留，含 SSE 测试） | 子代理 A 引用图谱 |
| F2 | model_name 仅 llm.rs：trait:45/分发:67/三 impl/测试断言:667/:787，生产零消费 | 同上 |
| F3 | render_entity_graph(mermaid.rs:88) 零调用（含测试）；render_cycle_diagram(:178) 仅测试 :302/:304 | 同上 |
| F4 | DependsOn/Extends/TypeReference 生产图从不构建；DependsOn 有 4 处真实消费（mermaid.rs:20 过滤/community.rs:89 权重/chunk.rs:125 依赖/impact.rs:146 白名单），Extends/TypeReference 消费全在死代码 render_entity_graph 内 | 同上 |
| F5 | output.format：schema.rs:121/:128/:134 仅定义，生产零读取；html.rs:278/:284 在测试模块；export 子命令独立提供 HTML | 同上 |
| F6 | wiki.template（schema.rs:27/:36/:45）全仓零消费；embed.dimension（:160/:173）零消费（API 推断维度） | 同上 |
| F7 | default_top_k：CLI 硬编码 `default_value="10"`（main.rs:91），execute_search 不读配置；clap default_value 无法区分显式传参 | 同上 |
| F8 | status（main.rs:228-233）只打印"就绪"+配置路径；lint.rs:30 已有完整产物健康检查（孤儿/断链/过时）可复用；main.rs:241-249 已有 source_roots 派生逻辑 | 同上 |
| F9 | **affected_modules 半接线**：lib.rs:308-342 计算 inc_result.affected_modules 但只传 changed_files+entity_changes 给 run_generation_filtered；依赖模块文档不会重生成 | 子代理 B |
| F10 | **test_e2e 差分盲区**：fixture 非 git 仓库，GitDiff 策略下 analyze_git_diff 失败→fallback_to_full，"只重写受影响页"断言走全量回退路径从未验证差分 | 子代理 B |
| F11 | MockProvider（llm.rs:509-550）忽略输入返回固定文本，无法按模块区分；api.md（markdown.rs:65-72 实体签名渲染）是内容级差分接缝；结构级接缝=增量 documents 只含变更模块+全局文档 | 子代理 B |
| F12 | 确定性来源：community.rs:23 LEIDEN_SEED=42 + :135-144 按最小 file_path 排序输出；feature.rs:25 seed=42 | 子代理 B |
| F13 | 删除 OutputFormat 连带 test_cli_smoke.rs:37；删 default_top_k 连带 :54；删 WikiTemplate 连带 test_multilang.rs:15（serde 宽松不报错，但应同步清理） | 子代理 A |
| F14 | tests/ 无共享 common 模块，fixture 复制粘贴惯例；cwd 竞态只存在于同一 test 二进制内，单测试文件可不加锁，多测试文件必须 CWD_LOCK+with_cwd | 子代理 B |
| F15 | cargo-machete 0.9.2（文本匹配，快，可 CI）；cargo-udeps 需 nightly；阻塞命令测试标准模式=spawn+try_wait 轮询+超时 kill+管道并发排空 | 网络查证 |
| F16 | 工作树唯一未提交改动：default-config.toml 的 embed 配置（enabled=true+阿里云百炼 base_url） | 子代理 A |

## 任务清单（依赖图见下）

### T0 基线清理（纯删除/接线，不改变生成行为）

**T0.1 删 A3 残留文件**
- 文件：`src/generate/card_test_regex.txt`（删）
- 验证：`cargo test` 数量不减

**T0.2 删 model_name 全链**（F2）
- 文件：`src/generate/llm.rs`（trait 声明:45、分发 match:67-73、OpenAi:144/Anthropic:339/Mock:528 impl、测试断言:667/:787）
- 注意：保留 complete_stream 全链（用户拍板）
- 验证：`cargo test --lib generate::llm` 全绿

**T0.3 删两个死渲染器**（F3）
- 文件：`src/output/mermaid.rs`（render_entity_graph:88-126、render_cycle_diagram:178-209、测试:302-304、清理失效导入）
- 验证：clippy 0 警告（死代码删除后无 unused import 残留）

**T0.4 删 3 种未构建边类型**（F4）
- 文件：`src/model/edge.rs`（枚举:34/:36/:38 + as_str:48-50）、`src/output/mermaid.rs:20`（过滤简化为 Imports|Calls）、`src/analysis/community.rs:89`（权重表删 DependsOn 分支）、`src/generate/chunk.rs:125`（依赖计算简化为 Imports）、`src/incremental/impact.rs:146`（白名单简化为 Imports|Calls）、3 个测试构造点（impact.rs:210/mermaid.rs:268/module.rs:237）、`CONTEXT.md`（7 种关系→4 种）
- **语义注意**：impact 传播白名单删除 DependsOn 后，传播仅靠 Imports/Calls——社区检测的 WEIGHT_DEPENDS_ON 分支同步删；chunk 依赖计算只剩 Imports（依赖摘要可能变化，需确认 wiki 页 references 不依赖 DependsOn 边——chunk.rs 依赖只影响卡片 dependencies 字段）
- 验证：全量测试 + clippy

**T0.5 删 output.format**（F5）
- 文件：`src/config/schema.rs`（字段:121/默认:128/枚举:134-137）、`src/output/html.rs`（测试 use:278/构造:284）、`default-config.toml`（output.format 键）、`tests/test_cli_smoke.rs:37`
- 验证：`cargo test --test test_cli_smoke` 全绿

**T0.6 删 wiki.template**（F6）
- 文件：`src/config/schema.rs`（字段:27/默认:36/枚举:45-48）、`default-config.toml`、`tests/test_multilang.rs:15`
- 验证：`cargo test --test test_multilang` 全绿

**T0.7 删 embed.dimension**（F6）
- 文件：`src/config/schema.rs`（字段:160/默认:173）、`src/search/semantic.rs`（测试构造:134/:150）、`default-config.toml` 注释行
- 验证：`cargo test --lib search::semantic` 全绿

**T0.8 default_top_k 接线**（F7）
- 文件：`src/main.rs`（:91 改 `Option<usize>`，去掉 default_value；:322 未传时 `config.search.default_top_k`）
- 边界：CLI 显式传 -k 优先于配置；配置默认 10 不变
- 测试：test_cli_smoke 增补"配置 default_top_k=5 且不传 -k 时结果数 ≤5"
- 注意：execute_search 的 top_k 参数类型不变（main 层回退后传入）

**T0.9 提交工作树 embed 配置**（F16）
- default-config.toml 的用户 embed 配置（enabled=true+百炼 base_url）——与 T0.5-T0.7 的删除合并提交，注释保留说明

### T1 status 健康检查（F8）

**T1.1 main.rs Status 分支重写**
- 复用 lint(output_dir, source_roots)：产物根不存在→提示"未生成，运行 generate"（退出码 0 或 1？——保持 0，生成提示类）；存在→统计 wiki 页/卡片数 + 孤儿/断链/过时数，有问题输出警告行
- 保持"Wiki 状态: 就绪"兼容文案（test_cli_smoke.rs:103-124 断言同步更新，新增健康统计断言）
- 实现放 `src/commands.rs` 新增 `fn status_report(config) -> Result<StatusReport>`（返回结构化数据，main 只做格式化——可测试性）
- 边界：无产物目录不 panic；lint 的 source_roots 复用 main.rs:241-249 派生逻辑

**T1.2 测试**
- test_cli_smoke 更新：就绪断言扩展为"页面数/卡片数存在"
- 新增场景：产物不存在时提示未生成；预置孤儿页时报告含孤儿数

### T2 传播闭环接线（F9）

**T2.1 反查函数**
- `incremental/impact.rs`（或 lib.rs）新增 `pub fn module_files(modules: &[String], graph: &KnowledgeGraph) -> Vec<PathBuf>`：模块名→ModuleCluster.node_ids→File 节点 file_path 反查（社区模块名与 ModuleCluster.name 精确匹配）
- 单测：小图断言反查正确 + 未知模块名返回空

**T2.2 run_generation_filtered 接线**
- `generate/mod.rs`：签名增加 `affected_modules: &[String]`（或并入现有参数）；insights 过滤改为 `changed_files ∪ module_files(affected_modules)`（去重）
- 边界：affected_modules 为空（全量回退场景）行为不变；模块反查无文件（改名后旧名）不炸

**T2.3 lib.rs 调用点**
- run_incremental_pipeline :341 传 `&inc_result.affected_modules`
- FileWatch 策略同样受益（affected_modules 已由 run_file_watch_incremental 计算）

**T2.4 测试**
- 单测：module_files 反查（T2.1）
- e2e：并入 T3.1 场景 D（签名变更→依赖模块重生成断言）

### T3 差分真实验证（F10/F11/F12）

**T3.1 tests/test_incremental_git_e2e.rs（新建，单文件多测试+CWD_LOCK+with_cwd）**
- fixture：复制 build_fixture_repo（test_e2e.rs:18-66）+ git2 init + 显式 Signature::now 首次提交（规避缺 user config）
- 场景 A（body 变更）：改 a 函数体→commit→增量→断言 a 模块文档在 documents 且 b 模块文档**不在**（结构级差分）+ api.md 内容与变更前相同（body 不影响签名）+ 页面集合不变
- 场景 B（签名变更）：改 a 签名→commit→增量→断言 api.md 含新签名不含旧签名（内容级）+ a 模块页重生成
- 场景 C（无变更）：再跑增量→documents.is_empty()
- 场景 D（T2.2 接线后）：a 签名变更→依赖 a 的 b 模块文档被重生成（需 fixture 构造 a→b 调用边——a 调 b 的函数，b 是依赖方?注意方向：传播从变更文件沿 Imports/Calls 双向→b 受影响。构造 a imports b 且 b 调 a?实际语义：**变更 a 的签名→调用 a 的 b 受影响**。fixture：b 调 a 的函数）
- 边界：git2 提交失败显式报错不静默；cwd 恢复用 with_cwd 闭包

### T4 评测三项断言（F12）

**T4.1 tests/test_determinism.rs（新建）：产物集合确定性快照**
- 两次 run_pipeline（同 config+cwd）→ 产物相对路径集合一致（排除 .state/、search 索引、_index.json）→ 逐文件内容哈希：wiki/*.md 归一化 "> 最后更新: " 行后相等，其余直接相等 → AnalysisResult 的 documents/cards 数量 + modules 名称排序相等
- 反向验证：预写人工编辑页→force=false 第二次运行该文件哈希不同（证明断言工具能捕获差异）
- 单测试函数 + CWD_LOCK

**T4.2 tests/test_clustering_stability.rs（新建）：聚类跨次稳定**
- 复制 bench 合成仓库最小版（10 簇×8 文件=80 文件，m{m}/f{i}.rs 完全图+跨簇单边）
- 断言：两次 detect_modules 模块数相等+名称排序相等+每模块 node_ids 排序相等；两次 detect_features 特征数+node_ids 集合相等；模块数在合理区间（8..=14）
- 单测试函数 + CWD_LOCK

### T5 信号量 + CLI 测试 + 依赖审计

**T5.1 describe_modules 加信号量**（D1）
- `src/generate/wiki.rs`：WikiGenerator 增加 `semaphore: Arc<Semaphore>` 字段（new 加 max_concurrent 参数——调用点两处：generate/mod.rs run_generation/run_generation_filtered 的 WikiGenerator::new）；describe_modules 的 join_all 任务内 acquire
- 边界：max_concurrent=0 语义与 card.rs 一致（usize::MAX）
- 测试：现有 wiki.rs 测试的 new 签名同步更新

**T5.2 update CLI 冒烟**
- test_cli_smoke 新增：mock generate 后 update（非 git 目录→回退全量）断言退出码 0；真实 git 仓库 update 断言差分（T3.1 已覆盖 lib 层，CLI 层仅冒烟）
- 复制 test_cli_smoke.rs:79-98 的 spawn 模式

**T5.3 watch CLI 冒烟**（F15 模式）
- test_cli_smoke 新增：spawn `repo-wiki watch`（std Command + try_wait 轮询 100ms + 超时 kill，管道用线程排空防阻塞）→ 等就绪输出 → 写一个源文件 → 轮询 .repo-wiki 产物变化（≤5s）→ kill
- 边界：Windows 下 kill 用 child.kill()（TerminateProcess）；超时即失败并清理子进程
- 风险标注：watch 测试有平台时序敏感性，断言宽松（产物目录出现即通过）

**T5.4 依赖审计**
- `cargo install cargo-machete` 后运行；检出未用依赖用 `--with-metadata` 复核（防文本匹配误报：宏/构建脚本用法）；确认后删除并补测试（若有测试专用依赖误报，加入 ignored 表）
- 已知候选：petgraph serde-1 feature（模型序列化在用）、ignore（scanner 用）——以 machete 实际输出为准

## 依赖图

```
T0.1-T0.3 ──┐
T0.5-T0.7 ──┤
T0.4 ───────┼──→ T0.9（统一提交+基线验证）
T0.8 ──────┘
T1.1 ─→ T1.2
T2.1 ─→ T2.2 ─→ T2.3 ─→ T2.4
T3.1 ─────────────────（场景 D 依赖 T2.2 完成）
T4.1、T4.2 ────────────（独立）
T5.1、T5.2、T5.3、T5.4 ─（独立，T5.2 场景 2 依赖 T3.1 fixture）
```

并行策略：T0.x 可并行（不同文件）；T1/T4/T5 独立并行；T2→T3 顺序。

## 雾区（Not yet specified，超出本计划决策）

- 真实 LLM 大仓库端到端验证（依赖外部 API 与运行时间，非本计划范围）
- embedding 注入路径的真实验证（依赖 API key；T0.9 提交的百炼配置可作为后续验证入口）
- watch 在真实 opencode 会话中的端到端行为（B1-B6 遗留项）
- 产物确定性快照的"文档内容稳定性"（LLM 输出天然不稳定，本计划只锁结构+非 LLM 产物）

## Out of scope

- complete_stream 保留为储备能力（用户拍板），不接线不删除
- 新功能开发（parser 扩展、新引擎等）
- .swarm/evidence 历史验证记录补写（文档缺口，以计划/STATUS 记录为准）

## 整体验收标准

1. `cargo test` 全绿且测试数 ≥ 基线 268 + 新增（预计 285-295），新增评测断言全部进回归门禁
2. `cargo clippy --all-targets -- -D warnings` 0 警告
3. `cargo machete` 0 未用依赖（或 ignored 表有依据）
4. 配置审计：default-config.toml 每个键均有消费代码（rg 可证）；status 输出健康统计
5. T2/T3 闭环：签名变更→依赖模块重生成有 e2e 断言（场景 D）
6. 每个 T 任务：验证方式注明（测试/命令），提交粒度：T0 一个 commit（或按文件组拆 2-3 个），T1/T2/T3/T4/T5 各一个 commit

## 防回归锚点（既有测试不可误删）

- test_cli_smoke.rs:103-124 status 断言（T1.1 需同步更新而非绕过）
- test_cli_smoke.rs:37/:54 配置文本（T0.5/T0.8 同步清理）
- test_multilang.rs:15（T0.6）
- impact.rs 测试的 DependsOn 构造（T0.4 改构造点语义，断言不变）
- wiki.rs 测试的 WikiGenerator::new（T5.1 签名变化，同步更新）
- 全部 268 基线测试删除任何一条前先确认删除理由（只允许随死代码一起删）
